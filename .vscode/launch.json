{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run program",
            "program": "${workspaceRoot}/target/dist/bundle.js",
            "preLaunchTask": "build",
            "outFiles": [
                "${workspaceRoot}/target/dist/**/*.js"
            ]
        }
    ]
}
