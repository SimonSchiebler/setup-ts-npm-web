{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run program",
            "program": "${workspaceRoot}/src/main.ts",
            "preLaunchTask": "compile",
            "outFiles": [
                "${workspaceRoot}/target/compile/**/*.js"
            ]
        }
    ]
}
